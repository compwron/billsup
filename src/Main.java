public class Main {
    public static void main(String... args) {
        BillCalculator billCalculator = new BillCalculator();
        billCalculator.addExpense(new Expense(amount, participants));
        billCalculator.fromPerspective(participant);
    }
}
